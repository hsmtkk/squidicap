--- src/adaptation/ServiceConfig.cc.orig	2021-03-13 04:53:59.000000000 +0000
+++ src/adaptation/ServiceConfig.cc	2021-03-13 04:55:10.000000000 +0000
@@ -14,6 +14,7 @@
 #include "Debug.h"
 #include "globals.h"
 #include "ip/tools.h"
+#include "ipcache.h"
 #include <set>
 
 Adaptation::ServiceConfig::ServiceConfig():
@@ -245,6 +246,7 @@
     }
 
     host.assign(s, len);
+    (void)ipcache_gethostbyname(host.termedBuf(), IP_LOOKUP_IF_MISS);
 #if USE_OPENSSL
     if (secure.sslDomain.isEmpty())
         secure.sslDomain.assign(host.rawBuf(), host.size());
